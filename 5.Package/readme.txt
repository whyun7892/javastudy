

when u compile, use 

	'javac -d . UserInfo.java'

and then,
	A new directory will be created within current directory.


If u suffer from some error, you may have to set environment by using..

	'export CLASSPATH="."'

